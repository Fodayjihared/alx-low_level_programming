#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * mul - Multiply two numbers
 * @a: The first number
 * @b: The second number
 *
 * Return: Always 1 (Success).
 */
int mul(int a, int b)
{
	return (a * b);
}
